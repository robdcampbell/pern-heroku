{
  "name": "pern-heroku",
  "version": "1.0.0",
  "description": "PERN stack project deployable to heroku",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rob Campbell",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^10.0.0"
  }
}
